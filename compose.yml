services:
  php81:
    container_name: php-unicode-lib-81
    build:
      context: ./
      dockerfile: ./.docker/Dockerfile
      args:
        PHP_VERSION: "8.1"
    tty: true
    working_dir: /app
    volumes:
      - ./:/app

  php82:
    container_name: php-unicode-lib-82
    build:
      context: ./
      dockerfile: ./.docker/Dockerfile
      args:
        PHP_VERSION: "8.2"
    tty: true
    working_dir: /app
    volumes:
      - ./:/app

  php83:
    container_name: php-unicode-lib-83
    build:
      context: ./
      dockerfile: ./.docker/Dockerfile
      args:
        PHP_VERSION: "8.3"
    tty: true
    working_dir: /app
    volumes:
      - ./:/app

  php84:
    container_name: php-unicode-lib-84
    build:
      context: ./
      dockerfile: ./.docker/Dockerfile
      args:
        PHP_VERSION: "8.4"
    tty: true
    working_dir: /app
    volumes:
      - ./:/app

  php85:
    container_name: php-unicode-lib-85
    build:
      context: ./
      dockerfile: ./.docker/Dockerfile
      args:
        PHP_VERSION: "8.5"
    tty: true
    working_dir: /app
    volumes:
      - ./:/app
